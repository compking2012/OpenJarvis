version: "3.1"

intents:
  - greet
  - goodbye

# ##################################################
# actions
# ##################################################
actions:
  - action_hello_world

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true